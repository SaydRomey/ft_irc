
DONE:

reply not registered
replies 2-3-4
removed NOTICE related code
Fixed QUIT to not need validation
	included QUIT to the single word no comma trailing edge case

Fixed USER for only one param
	(still allows 4 params, need to check getter takes proper value...)

in validate Kick, should we flag more than one channel ?

TODO:

Server name, localhost or ircserv ? (fix in templates)

cleanup!!


in Server::run()

[...]
else
	..."QUIT :Obsolete message?...


#### for channel name

in Validate JOIN command:
	// should we ignore trailing if present, or flag the error? **


Check in joinMsg and partMsg for the format of the nickname... **

